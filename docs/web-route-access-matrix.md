# Web Route Access Matrix (Archived)

هذا الملف أصبح أرشيفيًا.

بتاريخ **2026-02-28** تم حذف طبقة **Flutter Web** من المشروع بالكامل (بما فيها `mobile/web` وملفات البناء المرتبطة بها)، تمهيدًا لاستبدالها مستقبلًا بصفحات HTML مستقلة.

## الوضع الحالي

- لا توجد مسارات Flutter Web فعالة داخل المستودع.
- لا يوجد ملف `mobile/lib/main_web.dart` ضمن الكود الحالي.
- واجهات الويب الحالية الفعالة هي صفحات Django:
  - `/dashboard/`
  - `/portal/extras/`

## ملاحظة

إذا تم بناء واجهة HTML مستقبلًا، يجب إنشاء مصفوفة وصول جديدة خاصة بها بدل الاعتماد على هذا المستند.
