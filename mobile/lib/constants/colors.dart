import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFFFFFFFF);
  static const Color softBlue = Color.fromARGB(255, 14, 18, 22);
  static const Color accentOrange = Color(0xFFF1A559);
  static const Color lightPurple = Color.fromARGB(255, 108, 46, 201);
  static const Color deepPurple = Color.fromARGB(255, 102, 61, 144);

  // ✅ الألوان الموحدة الجديدة
  static const Color primaryLight = Color(0xFFE6E6FA); // الأصلي
  static const Color primaryDark = Color.fromARGB(
    255,
    118,
    118,
    173,
  ); // أغمق بنسبة 25%
}
